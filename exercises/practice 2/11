def factorial(n: int) -> int:
    """Calculates factorial of int n"""

    # edge case
    if n < 0:
        return -1
    # base case
    if n == 0 or n == 1:
        return 1
    # recursive case
    if n > 0:
        return n * factorial(n - 1)
