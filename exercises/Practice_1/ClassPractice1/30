def check_first_letter(word: str, letter: str) -> str:
"""Check if first letter in word matches letter param"""
    if len(letter) != 1:
        return "letter's argument should be one character!"
    elif word[0] == letter:
        return "match!"
    else:
        return "no match!"
